name: 构建 Windows EXE

on:
  workflow_dispatch:  # 手动触发
  push:
    tags:
      - 'v*'  # 当推送版本标签时自动构建

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v4
      with:
        submodules: false  # 禁用submodule，避免submodule配置错误
        fetch-depth: 1  # 只获取最新提交，减少submodule处理
    
    - name: 禁用submodule处理
      run: |
        # 完全禁用submodule处理
        git config --global submodule.recurse false
        git config --global --unset-all submodule.active 2>$null || echo "已清理"
        # 删除可能存在的.gitmodules引用
        if (Test-Path .gitmodules) {
          Remove-Item .gitmodules -Force -ErrorAction SilentlyContinue
        }
        # 清理submodule缓存
        git rm --cached -r "1.原文件/LLM_resume_filter" 2>$null || echo "已清理"
        git rm --cached -r "7.LLM_resume_filter" 2>$null || echo "已清理"
      continue-on-error: true  # 即使失败也继续
      shell: pwsh
    
    - name: 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        cd "5.界面/条件较为简单+多行表"
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: 构建 EXE（使用spec文件）
      run: |
        cd "5.界面/条件较为简单+多行表"
        pyinstaller build_exe.spec --clean
      env:
        PYTHONIOENCODING: utf-8
    
    - name: 创建部署包目录结构
      shell: cmd
      run: |
        cd "5.界面/条件较为简单+多行表"
        if not exist "部署包" mkdir "部署包"
        copy dist\AI简历初筛系统.exe 部署包\
        if not exist "部署包\data" mkdir "部署包\data"
        if not exist "部署包\output" mkdir "部署包\output"
        if not exist "部署包\logs" mkdir "部署包\logs"
    
    - name: 上传构建产物（EXE文件）
      uses: actions/upload-artifact@v4
      with:
        name: windows-exe-standalone
        path: "5.界面/条件较为简单+多行表/dist/AI简历初筛系统.exe"
        retention-days: 30
    
    - name: 上传完整部署包
      uses: actions/upload-artifact@v4
      with:
        name: windows-deployment-package
        path: "5.界面/条件较为简单+多行表/部署包/"
        retention-days: 30
    
    - name: 显示构建信息
      shell: cmd
      run: |
        cd "5.界面/条件较为简单+多行表"
        echo "✅ 构建完成！"
        echo "📦 EXE文件位置: dist/AI简历初筛系统.exe"
        if exist "dist\AI简历初筛系统.exe" (
            dir dist\AI简历初筛系统.exe
            echo "文件大小:"
            for %%A in (dist\AI简历初筛系统.exe) do echo %%~zA 字节
        )
