# 🔧 解决工作流不显示的问题

## 问题现象
在 GitHub Actions 页面左侧看不到 "构建 Windows EXE" 工作流。

## 🔍 排查步骤

### 步骤 1：确认文件是否存在

访问以下链接检查文件是否存在：
```
https://github.com/qiumeng0922/resume_extractor/tree/master/.github/workflows
```

**应该能看到**：
- `build-windows-exe.yml` 文件

**如果看不到**：
- 文件还没有创建，需要先创建（见下面的解决方案）

---

### 步骤 2：检查文件路径

文件路径必须是：
```
.github/workflows/build-windows-exe.yml
```

**注意**：
- `.github` 前面有一个点（隐藏目录）
- `workflows` 是复数形式
- 文件名是 `build-windows-exe.yml`（注意是横线，不是下划线）

---

### 步骤 3：检查文件内容格式

文件必须是有效的 YAML 格式，第一行应该是：
```yaml
name: 构建 Windows EXE
```

---

## ✅ 解决方案

### 方案 1：直接访问工作流页面（最快）

如果文件已经存在，直接访问：
```
https://github.com/qiumeng0922/resume_extractor/actions/workflows/build-windows-exe.yml
```

如果页面能打开，说明文件存在，可以直接点击 "Run workflow"。

---

### 方案 2：使用 "New workflow" 创建

1. **在 Actions 页面**
   - 访问：https://github.com/qiumeng0922/resume_extractor/actions
   - 点击左侧绿色的 **"New workflow"** 按钮

2. **选择 "set up a workflow yourself"**
   - 点击页面底部的链接：**"Skip this and set up a workflow yourself →"**
   - 或者点击 "set up a workflow yourself" 按钮

3. **删除默认内容**
   - 删除编辑器中的所有默认内容

4. **粘贴工作流内容**
   - 复制下面的完整内容
   - 粘贴到编辑器中

5. **提交文件**
   - 点击 "Start commit"
   - 填写提交信息："添加Windows EXE打包工作流"
   - 点击 "Commit new file"

---

### 方案 3：通过 Code 页面创建

1. **访问 Code 页面**
   - https://github.com/qiumeng0922/resume_extractor

2. **创建目录结构**
   - 点击 "Add file" → "Create new file"
   - 在路径框中输入：`.github/workflows/build-windows-exe.yml`
   - GitHub 会自动创建目录

3. **粘贴内容并提交**
   - 复制下面的工作流内容
   - 粘贴到编辑器
   - 点击 "Commit new file"

---

## 📋 完整的工作流内容（复制这个）

```yaml
name: 构建 Windows EXE

on:
  workflow_dispatch:  # 手动触发
  push:
    tags:
      - 'v*'  # 当推送版本标签时自动构建

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: 检出代码
      uses: actions/checkout@v3
    
    - name: 设置 Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        cd "5.界面/条件较为简单+多行表"
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: 构建 EXE（使用spec文件）
      run: |
        cd "5.界面/条件较为简单+多行表"
        pyinstaller build_exe.spec --clean
      env:
        PYTHONIOENCODING: utf-8
    
    - name: 创建部署包目录结构
      run: |
        cd "5.界面/条件较为简单+多行表"
        if not exist "部署包" mkdir "部署包"
        copy dist\AI简历初筛系统.exe 部署包\
        if not exist "部署包\data" mkdir "部署包\data"
        if not exist "部署包\output" mkdir "部署包\output"
        if not exist "部署包\logs" mkdir "部署包\logs"
    
    - name: 上传构建产物（EXE文件）
      uses: actions/upload-artifact@v3
      with:
        name: windows-exe-standalone
        path: "5.界面/条件较为简单+多行表/dist/AI简历初筛系统.exe"
        retention-days: 30
    
    - name: 上传完整部署包
      uses: actions/upload-artifact@v3
      with:
        name: windows-deployment-package
        path: "5.界面/条件较为简单+多行表/部署包/"
        retention-days: 30
    
    - name: 显示构建信息
      run: |
        cd "5.界面/条件较为简单+多行表"
        echo "✅ 构建完成！"
        echo "📦 EXE文件位置: dist/AI简历初筛系统.exe"
        if exist "dist\AI简历初筛系统.exe" (
            dir dist\AI简历初筛系统.exe
            echo "文件大小:"
            for %%A in (dist\AI简历初筛系统.exe) do echo %%~zA 字节
        )
```

---

## 🎯 推荐操作流程

### 最简单的方法：

1. **直接访问工作流页面**
   ```
   https://github.com/qiumeng0922/resume_extractor/actions/workflows/build-windows-exe.yml
   ```

2. **如果页面显示 404 或不存在**
   - 说明文件还没有创建
   - 使用方案 2（New workflow）创建

3. **如果页面能打开**
   - 直接点击 "Run workflow" 按钮
   - 选择分支：`master`
   - 点击绿色按钮运行

---

## ⚠️ 常见问题

### 问题 1：文件创建后还是看不到

**解决方法**：
- 等待 1-2 分钟（GitHub 需要时间索引）
- 刷新页面（F5 或 Cmd+R）
- 清除浏览器缓存

### 问题 2：点击 "New workflow" 后找不到选项

**解决方法**：
- 点击 "Skip this and set up a workflow yourself"
- 手动创建文件

### 问题 3：文件格式错误

**检查项**：
- 确保是 YAML 格式（缩进使用空格，不是 Tab）
- 确保第一行是 `name: 构建 Windows EXE`
- 确保文件扩展名是 `.yml` 或 `.yaml`

---

## 📝 验证文件是否创建成功

创建文件后，访问以下链接验证：
```
https://github.com/qiumeng0922/resume_extractor/blob/master/.github/workflows/build-windows-exe.yml
```

如果能看到文件内容，说明创建成功。

---

**先尝试直接访问工作流页面，如果不存在，再用方案 2 创建！** 🚀
