# 解决 GitHub 推送网络问题

## 🔍 当前状态

✅ 远程仓库已配置：`https://github.com/qiumeng0922/resume_extractor.git`  
✅ 本地代码已提交  
❌ 推送时遇到网络连接错误

## 🚀 解决方案

### 方案 1：使用 SSH 方式（推荐，更稳定）

如果已配置 SSH 密钥，使用 SSH 方式推送更稳定：

```bash
cd "/Users/ameng/Documents/projects/11.AI简历可行性评估"

# 切换到SSH地址
git remote set-url origin git@github.com:qiumeng0922/resume_extractor.git

# 推送代码
git push -u origin master
```

**如果没有SSH密钥**，可以生成一个：
```bash
# 生成SSH密钥（如果还没有）
ssh-keygen -t ed25519 -C "your_email@example.com"

# 复制公钥
cat ~/.ssh/id_ed25519.pub

# 将公钥添加到GitHub：
# 1. 访问 https://github.com/settings/keys
# 2. 点击 "New SSH key"
# 3. 粘贴公钥内容
```

---

### 方案 2：使用 GitHub CLI 推送

GitHub CLI 已经登录，可以直接推送：

```bash
cd "/Users/ameng/Documents/projects/11.AI简历可行性评估"

# 使用gh推送（会自动使用已认证的凭据）
gh repo sync qiumeng0922/resume_extractor --source . --branch master
```

或者直接推送：
```bash
git push -u origin master
# 如果还是失败，尝试：
GIT_ASKPASS=echo git push -u origin master
```

---

### 方案 3：分批推送（如果文件太大）

如果项目文件很大，可以尝试分批推送：

```bash
cd "/Users/ameng/Documents/projects/11.AI简历可行性评估"

# 先推送最近的几个提交
git push -u origin master --depth=1

# 或者只推送当前分支
git push origin master --force-with-lease
```

---

### 方案 4：使用代理（如果网络受限）

如果在中国大陆，可能需要使用代理：

```bash
# 设置Git代理（如果有代理）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 推送
git push -u origin master

# 推送完成后，取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

---

### 方案 5：使用 GitHub Desktop（图形界面）

如果命令行一直失败，可以使用 GitHub Desktop：

1. 下载安装：https://desktop.github.com/
2. 登录你的 GitHub 账号
3. 添加本地仓库
4. 点击 "Push origin" 按钮

---

## 🔧 已优化的 Git 配置

我已经为你优化了 Git 配置：

```bash
# 增加缓冲区大小（处理大文件）
git config --global http.postBuffer 524288000

# 降低超时限制（网络慢时不会太快失败）
git config --global http.lowSpeedLimit 0
git config --global http.lowSpeedTime 999999

# 使用 HTTP/1.1（避免 HTTP/2 问题）
git config --global http.https://github.com/.httpVersion HTTP/1.1
```

---

## 📝 推荐操作步骤

### 步骤 1：尝试 SSH 方式

```bash
cd "/Users/ameng/Documents/projects/11.AI简历可行性评估"

# 检查是否有SSH密钥
ls -la ~/.ssh/id_*

# 如果有SSH密钥，切换到SSH地址
git remote set-url origin git@github.com:qiumeng0922/resume_extractor.git

# 推送
git push -u origin master
```

### 步骤 2：如果SSH不可用，尝试直接推送

```bash
# 直接推送（使用已优化的配置）
git push -u origin master
```

### 步骤 3：如果还是失败

- 检查网络连接
- 尝试使用手机热点
- 或者使用 GitHub Desktop 图形界面

---

## ✅ 推送成功后

一旦推送成功，就可以：

1. **打开 GitHub 网页**
   - 访问：https://github.com/qiumeng0922/resume_extractor

2. **进入 Actions 标签**
   - 点击仓库顶部的 **Actions** 标签

3. **运行工作流**
   - 找到 **"构建 Windows EXE"** 工作流
   - 点击 **"Run workflow"** 按钮
   - 等待构建完成
   - 下载生成的 exe 文件

---

## 🆘 如果所有方法都失败

可以考虑：
1. 使用 GitHub Desktop（图形界面，更稳定）
2. 使用手机热点（排除网络问题）
3. 联系网络管理员（如果在公司网络）

---

**先尝试 SSH 方式，通常最稳定！** 🚀
